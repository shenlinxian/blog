<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
    <section>
      <%= render 'shared/stats' %>
    </section>
  </aside>
  <div class="span8">
    <%= render 'follow_form' if signed_in? %>
    <% if @user.passages.any? %>
      <h3>passages (<%= @user.passages.count %>)</h3>
      <ol class="passages">
        <%= render @passages %>
      </ol>
      <%= will_paginate @passages %>
    <% end %>
  </div>
</div>